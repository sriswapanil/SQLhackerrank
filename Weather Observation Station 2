Query the following two values from the STATION table:

1. The sum of all values in LAT_N rounded to a scale of 4 decimal places.
2. The sum of all values in LONG_W rounded to a scale of 4 decimal places.

solution

select round(sum(lat_n), 2), round(sum(long_w), 2) from station